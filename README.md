# MatlabParametricEQ1
5-band parametric equalizer built as a MATLAB GUI application using uifigure components. It allows users to load a WAV audio file, shape its frequency response through five independent filter bands, and play back the processed audio in real time — all within a dark-themed, studio-inspired interface.
